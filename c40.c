#include<stdio.h>
int main(){
    int xptr;
    int n,i;
ptr = malloc(n*size of (int));
if (ptr== null)
print("insufficient memory");
return 
    
}
//diff bw compile memory & runtime memory
//memory is allocated during compilation (cm)
//we cannot reuse memory
//no library function required
//no modification of memory

//memory is allocated during runtime (rm)
//we can reuse memory & releasing memory using free()
//library function malloc
//modification of memory










syntax-- 
void*malloc(number of bytes);
void*pointer(generic pointer);
int* is typecast require as i mentioned malloc  return void starhence to 

